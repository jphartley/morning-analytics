## 1. Add Position Indicator

- [ ] 1.1 Add a position counter element to Lightbox.tsx JSX, positioned at bottom center of the overlay using absolute positioning
- [ ] 1.2 Display "{currentIndex + 1} of {imageUrls.length}" text inside a semi-transparent dark pill (bg-black/60 text-white, rounded-full, px/py padding, text-sm)

## 2. Verify Behavior

- [ ] 2.1 Verify counter updates correctly when navigating via arrow buttons and keyboard arrows (uses existing currentIndex state)
- [ ] 2.2 Run build and lint to confirm no regressions
